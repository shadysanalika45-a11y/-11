package feathers.core
{
   public interface IFocusManagerAware
   {
      
      function set_focusManager(param1:IFocusManager) : IFocusManager;
      
      function set focusManager(param1:IFocusManager) : void;
      
      function get_focusManager() : IFocusManager;
      
      function get focusManager() : IFocusManager;
   }
}

